nothing = ->

{HttpServer} = require('./../modules/net/http.js')
client = HttpServer.create! '127.0.0.1:9090'
client.sendHeader()


nothing!
{benchmark} = require('./../modules/core/benchmark.js')
JSON.encode = ->
  JSON.stringify.apply JSON, arguments

JSON.decode = ->
  JSON.parse.apply JSON.arguments

object =
  a: :hello
  b: :world
  c:
    d: :hello
    e: :world


header = """GET / HTTP/1.1\r\nUser-Agent: curl/7.22.0 (i686-pc-linux-gnu) libcurl/7.22.0 OpenSSL/1.0.0e zlib/1.2.5 libssh2/1.3.0\r\nHost: www.baidu.com\r\nAccept: */*\r\n\r\n"""



benchmark 'parseHeader', ->
  parseHeader(header)

nothing!

data = null
benchmark 'stringify', ->
  for i in [1..times]
    data = JSON.stringify(object)

benchmark 'decode', ->
  for i in [1..times]
    object = JSON.stringify(data)

benchmark 'eval', ->
  for i in [1..times]
    object = eval("(#{data})")
